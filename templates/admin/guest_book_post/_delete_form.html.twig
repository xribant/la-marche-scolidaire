<form method="post" action="{{ path('guest_book_post_delete', {'id': guest_book_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guest_book_post.id) }}">
    <button class="btn">Delete</button>
</form>
